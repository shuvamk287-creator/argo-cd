apiVersion: v1
kind: Service

metadata:
  name: ai-cost-service

spec:
  type: NodePort

  selector:
    app: ai-cost-detective

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080